function MainMenu(){
  
  this.show = function(){
    push()
    background(images.menuwallpaper);
    fill(255);
    textSize(50);
    textAlign(CENTER)

    // startgame = createButton('Start game')

    pop();
  }

}